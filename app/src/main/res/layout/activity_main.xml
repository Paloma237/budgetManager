<?xml version="1.0" encoding="utf-8"?>
<!--
  Écran hôte : un conteneur (FrameLayout) qui affiche le fragment courant,
  et une barre de navigation (BottomNavigationView) fixée en bas.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blanc"
        app:itemIconTint="@color/bleu_principal"
        app:itemTextColor="@color/bleu_principal"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
